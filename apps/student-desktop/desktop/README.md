make sure you are in .venv first before everything else

run
```python
<!-- @/apps/student-desktop/frontend/ -->
npm install
npm run dev
<!-- keep it running in background, then open new terminal -->
```

run
```python
<!-- @/apps/student-desktop/desktop -->
python3 -m pip install -r requirements.txt
python3 main.py
```